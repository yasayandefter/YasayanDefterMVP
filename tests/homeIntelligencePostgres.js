"use strict";const assert=require("node:assert/strict"),crypto=require("node:crypto"),{spawn}=require("node:child_process"),{Pool}=require("pg");if(!process.env.TEST_DATABASE_URL){console.log("SKIP  home intelligence PostgreSQL: TEST_DATABASE_URL missing");process.exit(0);}if(process.env.DATABASE_URL&&process.env.DATABASE_URL!==process.env.TEST_DATABASE_URL)throw Error("TEST_DATABASE_URL_ONLY");const pool=new Pool({connectionString:process.env.TEST_DATABASE_URL}),suffix=crypto.randomBytes(5).toString("hex"),names={user:`home_u_${suffix}`,other:`home_o_${suffix}`,studentA:`home_sa_${suffix}`,studentB:`home_sb_${suffix}`,teacher:`home_t_${suffix}`},password="Home-test!";let child;async function wait(base){for(let i=0;i<100;i++){try{if((await fetch(base+"/api/status")).ok)return;}catch{}await new Promise(r=>setTimeout(r,100));}throw Error("TIMEOUT");}async function call(base,path,method="GET",body,cookie){const r=await fetch(base+path,{method,headers:{Accept:"application/json",Origin:base,...(body?{"Content-Type":"application/json"}:{}),...(cookie?{Cookie:cookie}:{})},body:body&&JSON.stringify(body)});return{response:r,body:await r.json().catch(()=>({}))};}async function add(base,cookie,title,type="note",metadata={}){const x=await call(base,"/api/memory","POST",{title,content:title,workspaceArea:"work",contentType:type,tags:["samsung","partner"],metadata},cookie);assert.equal(x.response.status,201);return x.body.memory;}async function stop(){if(child&&child.exitCode===null){child.kill("SIGTERM");await new Promise(r=>child.once("exit",r));}}
(async()=>{const port=49100+crypto.randomInt(400),base=`http://127.0.0.1:${port}`,start=()=>spawn(process.execPath,["server.js"],{cwd:process.cwd(),env:{...process.env,PORT:String(port),ACCESS_MODE:"authenticated",AUTH_MODE:"production",STORAGE_MODE:"postgres",DATABASE_URL:process.env.TEST_DATABASE_URL,APP_ORIGIN:base,NODE_ENV:"test"},stdio:["ignore","ignore","ignore"]});child=start();await wait(base);const sessions={},users={};for(const [key,name]of Object.entries(names)){const x=await call(base,"/api/auth/register","POST",{username:name,password});assert.equal(x.response.status,201);sessions[key]=String(x.response.headers.get("set-cookie")).split(";")[0];users[key]=x.body.user.id;}await pool.query("UPDATE users SET role='STUDENT' WHERE id=ANY($1::uuid[])",[[users.studentA,users.studentB]]);await pool.query("UPDATE users SET role='TEACHER' WHERE id=$1",[users.teacher]);for(const key of ["studentA","studentB"]){users[key+"Profile"]=crypto.randomUUID();await pool.query("INSERT INTO students(id,user_id,display_name) VALUES($1,$2,$3)",[users[key+"Profile"],users[key],names[key]]);}const records=[await add(base,sessions.user,"Samsung toplantı","meeting"),await add(base,sessions.user,"Samsung araştırma","research"),await add(base,sessions.user,"Samsung aktif proje","project",{status:"active"}),await add(base,sessions.user,"Samsung fikir","idea")];await call(base,"/api/collections","POST",{name:"Samsung Çalışmaları",workspaceArea:"work",recordIds:records.slice(0,3).map(x=>x.id)},sessions.user);const foreign=await add(base,sessions.other,"Samsung yabancı");let result=await call(base,"/api/intelligence/home","GET",null,sessions.user);assert.equal(result.response.status,200);assert.ok(result.body.home.continueItems.some(x=>/Samsung/i.test(x.title)));assert.equal(result.body.home.collectionHighlights[0].name,"Samsung Çalışmaları");assert.ok(result.body.meta.durationMs<750);assert.ok(!JSON.stringify(result.body).includes(foreign.id));assert.equal((await call(base,"/api/intelligence/home?ownerUserId="+users.other,"GET",null,sessions.user)).response.status,200);for(const key of ["studentA","studentB","teacher"]){await add(base,sessions[key],key+" Samsung A");await add(base,sessions[key],key+" Samsung B");}const studentHome=await call(base,"/api/intelligence/home","GET",null,sessions.studentA),teacherHome=await call(base,"/api/intelligence/home","GET",null,sessions.teacher);assert.ok(!JSON.stringify(studentHome.body).includes("studentB"));assert.ok(!JSON.stringify(teacherHome.body).includes("studentA"));assert.equal((await call(base,"/api/intelligence/home")).response.status,401);await stop();child=start();await wait(base);result=await call(base,"/api/intelligence/home","GET",null,sessions.user);assert.ok(result.body.home.continueItems.length);console.log("PASS  home PostgreSQL USER/STUDENT/TEACHER privacy, collections, restart consistency, spoof resistance and latency");})().catch(e=>{console.error(e.stack||e);process.exitCode=1;}).finally(async()=>{await stop();await pool.query("DELETE FROM users WHERE username=ANY($1::text[])",[Object.values(names)]).catch(()=>{});await pool.end();});
