"use strict";
const db=require("../db"),crypto=require("node:crypto"),{mapDatabaseError}=require("./errors"),memory=require("./memoryRepository"),{normalize}=require("../services/contextIntelligence");
function scope(owner,alias="c"){return owner&&owner.kind==="user"?{column:`${alias}.owner_user_id`,id:owner.id,userId:owner.id,studentId:null}:{column:`${alias}.student_id`,id:owner,userId:null,studentId:owner};}function map(row){return row&&{id:row.id,name:row.name,description:row.description||"",workspaceArea:row.workspace_area||"",recordCount:Number(row.record_count||0),createdAt:row.created_at instanceof Date?row.created_at.toISOString():row.created_at,updatedAt:row.updated_at instanceof Date?row.updated_at.toISOString():row.updated_at};}
async function list(owner,options={},client=db){const s=scope(owner),order={updated:"c.updated_at DESC,c.id",name:"LOWER(c.name),c.id",count:"record_count DESC,c.updated_at DESC"}[options.sort]||"c.updated_at DESC,c.id",values=[s.id],filter=[];if(options.q){values.push(`%${String(options.q).toLocaleLowerCase("tr-TR")}%`);filter.push(`(LOWER(c.name) LIKE $${values.length} OR LOWER(c.description) LIKE $${values.length})`);}values.push(Math.min(50,options.limit||20));try{return(await client.query(`SELECT c.*,COUNT(i.memory_record_id)::int record_count FROM smart_collections c LEFT JOIN smart_collection_items i ON i.collection_id=c.id WHERE ${s.column}=$1 ${filter.length?`AND ${filter.join(" AND ")}`:""} GROUP BY c.id ORDER BY ${order} LIMIT $${values.length}`,values)).rows.map(map);}catch(e){throw mapDatabaseError(e,"COLLECTION_LIST_FAILED");}}
async function findOwned(id,owner,client=db){const s=scope(owner);try{const result=await client.query(`WITH target AS (SELECT id FROM smart_collections WHERE id=$1), owned AS (SELECT c.* FROM smart_collections c WHERE c.id=$1 AND ${s.column}=$2) SELECT EXISTS(SELECT 1 FROM target) exists,(SELECT row_to_json(owned) FROM owned) collection`,[id,s.id]);const row=result.rows[0]||{};if(!row.collection)return{exists:Boolean(row.exists),collection:null,members:[]};const members=(await client.query(`SELECT m.* FROM smart_collection_items i JOIN memory_records m ON m.id=i.memory_record_id JOIN smart_collections c ON c.id=i.collection_id WHERE i.collection_id=$1 AND ${s.column}=$2 ORDER BY i.added_at,m.id LIMIT 100`,[id,s.id])).rows.map(memory.mapMemory);return{exists:true,collection:{...map(row.collection),recordCount:members.length},members};}catch(e){throw mapDatabaseError(e,"COLLECTION_DETAIL_FAILED");}}
async function create(owner,value,client=db){const s=scope(owner),id=crypto.randomUUID(),recordIds=value.recordIds;try{const result=await client.query(`WITH owned AS (SELECT id FROM memory_records m WHERE id=ANY($7::uuid[]) AND ${scope(owner,"m").column}=$1), valid AS (SELECT COUNT(*)::int n FROM owned HAVING COUNT(*)=$8), inserted AS (INSERT INTO smart_collections(id,owner_user_id,student_id,name,normalized_name,description,workspace_area) SELECT $2,$3,$4,$5,$6,$9,$10 FROM valid RETURNING *), items AS (INSERT INTO smart_collection_items(collection_id,memory_record_id) SELECT inserted.id,owned.id FROM inserted CROSS JOIN owned RETURNING memory_record_id) SELECT (SELECT row_to_json(inserted) FROM inserted) collection,(SELECT COUNT(*)::int FROM items) record_count`,[s.id,id,s.userId,s.studentId,value.name,normalize(value.name),recordIds,recordIds.length,value.description,value.workspaceArea]);const row=result.rows[0];return row?.collection?{collection:map({...row.collection,record_count:row.record_count}),foreign:false}:{collection:null,foreign:true};}catch(e){throw mapDatabaseError(e,"COLLECTION_CREATE_FAILED");}}
async function update(id,owner,value,client=db){const s=scope(owner);try{const result=await client.query(`WITH target AS (SELECT id FROM smart_collections WHERE id=$1), updated AS (UPDATE smart_collections c SET name=COALESCE($3,name),normalized_name=CASE WHEN $3 IS NULL THEN normalized_name ELSE $4 END,description=COALESCE($5,description),workspace_area=CASE WHEN $6::boolean THEN $7 ELSE workspace_area END,updated_at=NOW() WHERE id=$1 AND ${s.column}=$2 RETURNING *) SELECT EXISTS(SELECT 1 FROM target) exists,(SELECT row_to_json(updated) FROM updated) collection`,[id,s.id,value.name||null,value.name?normalize(value.name):null,value.description===undefined?null:value.description,Object.hasOwn(value,"workspaceArea"),value.workspaceArea]);return{exists:Boolean(result.rows[0]?.exists),collection:map(result.rows[0]?.collection)};}catch(e){throw mapDatabaseError(e,"COLLECTION_UPDATE_FAILED");}}
async function addItems(id,owner,recordIds,client=db){const s=scope(owner),ms=scope(owner,"m");try{const result=await client.query(`WITH collection AS (SELECT id FROM smart_collections c WHERE id=$1 AND ${s.column}=$2), owned AS (SELECT id FROM memory_records m WHERE id=ANY($3::uuid[]) AND ${ms.column}=$2), capacity AS (SELECT collection.id FROM collection WHERE (SELECT COUNT(*) FROM smart_collection_items WHERE collection_id=$1)+(SELECT COUNT(*) FROM owned)<=100), added AS (INSERT INTO smart_collection_items(collection_id,memory_record_id) SELECT capacity.id,owned.id FROM capacity CROSS JOIN owned ON CONFLICT DO NOTHING RETURNING memory_record_id) SELECT (SELECT COUNT(*) FROM owned)::int owned_count,(SELECT COUNT(*) FROM added)::int added_count,EXISTS(SELECT 1 FROM collection) collection_exists`,[id,s.id,recordIds]);return result.rows[0];}catch(e){throw mapDatabaseError(e,"COLLECTION_ADD_FAILED");}}
async function removeItem(id,recordId,owner,client=db){const s=scope(owner);try{const result=await client.query(`WITH collection AS (SELECT id FROM smart_collections c WHERE id=$1 AND ${s.column}=$3), removed AS (DELETE FROM smart_collection_items WHERE collection_id IN(SELECT id FROM collection) AND memory_record_id=$2 RETURNING memory_record_id) SELECT EXISTS(SELECT 1 FROM smart_collections WHERE id=$1) exists,EXISTS(SELECT 1 FROM collection) owned,EXISTS(SELECT 1 FROM removed) removed`,[id,recordId,s.id]);return result.rows[0];}catch(e){throw mapDatabaseError(e,"COLLECTION_REMOVE_FAILED");}}
async function deleteOwned(id,owner,client=db){const s=scope(owner);try{const result=await client.query(`WITH target AS(SELECT id FROM smart_collections WHERE id=$1),deleted AS(DELETE FROM smart_collections c WHERE id=$1 AND ${s.column}=$2 RETURNING id) SELECT EXISTS(SELECT 1 FROM target) exists,EXISTS(SELECT 1 FROM deleted) deleted`,[id,s.id]);return result.rows[0];}catch(e){throw mapDatabaseError(e,"COLLECTION_DELETE_FAILED");}}
async function relatedMemberIds(recordId,owner,client=db){const s=scope(owner);try{return new Set((await client.query(`SELECT DISTINCT other.memory_record_id id FROM smart_collection_items own JOIN smart_collections c ON c.id=own.collection_id JOIN smart_collection_items other ON other.collection_id=own.collection_id WHERE own.memory_record_id=$1 AND ${s.column}=$2 AND other.memory_record_id<>$1 LIMIT 100`,[recordId,s.id])).rows.map(x=>x.id));}catch(e){throw mapDatabaseError(e,"COLLECTION_CONTEXT_FAILED");}}
module.exports={name:"smartCollections",scope,map,list,findOwned,create,update,addItems,removeItem,deleteOwned,relatedMemberIds};
