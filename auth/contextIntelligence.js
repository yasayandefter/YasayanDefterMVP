"use strict";
const smart=require("./smartNote");
function invalid(){const e=new Error("INVALID_CONTEXT_REQUEST");e.code="INVALID_CONTEXT_REQUEST";return e;}function text(value,max){if(value===undefined||value===null)return"";if(typeof value!=="string"||value.length>max)throw invalid();return value.trim();}function list(value,max,count){if(value===undefined)return[];if(!Array.isArray(value)||value.length>count)throw invalid();return value.map(x=>text(x,max)).filter(Boolean);}
function suggest(value){if(!value||typeof value!=="object"||Array.isArray(value))throw invalid();const allowed=new Set(["query","title","summary","normalizedTopic","tags","keyConcepts","workspaceArea","contentType"]);if(Object.keys(value).some(k=>!allowed.has(k)))throw invalid();const workspaceArea=value.workspaceArea===undefined?"":text(value.workspaceArea,40),contentType=value.contentType===undefined?"":text(value.contentType,40);if(workspaceArea&&!smart.WORKSPACE_AREAS.includes(workspaceArea))throw invalid();if(contentType&&!smart.CONTENT_TYPES.includes(contentType))throw invalid();const result={query:text(value.query,240),title:text(value.title,120),summary:text(value.summary,4000),normalizedTopic:text(value.normalizedTopic,240),tags:list(value.tags,32,8),keyConcepts:list(value.keyConcepts,120,20),workspaceArea,contentType};if(!result.query&&!result.title&&!result.summary)throw invalid();return result;}
module.exports={suggest,invalid};
